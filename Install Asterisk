$ apt-get update -y
$ apt-get upgrade -y
$ apt-get install asterisk -y

Kemudian kalian konfigurasi untuk SIP nya

$ nano /etc/asterisk/sip.conf

Ke barisan paling bawah (kalau mau cepat tejan tombol pgdn)

